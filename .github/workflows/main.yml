name: Greeting Java

on:
  workflow_dispatch:

jobs:
  greeting-java-job:
    name: Greeting Java Job
    runs-on: self-hosted
    
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Build with Maven Wrapper
        shell: cmd
        run: F:\tools\maven-3\bin\mvn clean package -DskipTests

      - name: Deploy war file
        run: copy target\*.war F:\tools\tomcat-11\webapps
        